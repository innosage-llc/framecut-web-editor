
// This component is deprecated.
// Functionality has been moved to PropertyHUD.tsx.
// It remains here only as a placeholder to avoid build errors if referenced elsewhere,
// but should not be used in the new layout.

import React from 'react';

const InspectorPanel: React.FC<any> = () => {
    return null;
};

export default InspectorPanel;
